function interset(principal, rate = 3.5, years = 1);

// Set the defalut value on formal parameters

// !!! Once we set a default value of a formal parameters, all the formal parameters after this
// should be setted the default value as well.
// !!NO!!: interset(principal, rate = 3.5, years)