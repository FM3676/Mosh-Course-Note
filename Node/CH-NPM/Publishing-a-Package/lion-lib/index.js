module.exports.add = (a, b) => a + b;
// npm init
// We need to create an npmjs accuont --> Using npm adduser or visit npmjs.com to sign up.
// Or if you already have it, using npm login

// And then run npm publish. !! Make sure the name of package is unique.