module.exports.add = (a, b) => a + b;

module.exports.multiply = (a, b) => a * b;
// Now we add a new feature.
// We can update our version in package.json by hand 

// run npm version major || npm version minior || npm version patch
// then run npm publish
